'use strict';

const db = require('../db');

const tableName = '...';

async function insert(param) {}
async function read(param) {}
async function insertOrReplace(param) {}

module.exports = {
  tableName,
  insert,
  insertOrReplace,
  read,
};
